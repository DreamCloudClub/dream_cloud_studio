# Dream Cloud Studio Environment Configuration
# =============================================
#
# This file shows the environment variables needed for the client application.
# AI service API keys (Anthropic, ElevenLabs, FAL.ai, Replicate) are stored
# securely as Supabase edge function secrets, NOT exposed to the browser.

# Supabase Configuration (required)
# These are the only credentials needed in the client
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# =============================================
# EDGE FUNCTION SECRETS (set via Supabase CLI or Dashboard)
# =============================================
# These are NOT set in .env - they are configured as Supabase secrets:
#
# supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
# supabase secrets set ELEVENLABS_API_KEY=sk_...
# supabase secrets set FAL_API_KEY=...
# supabase secrets set REPLICATE_API_TOKEN=r8_...
#
# The edge functions (anthropic-proxy, elevenlabs-proxy, fal-proxy, replicate-proxy)
# read these secrets securely on the server side.

# =============================================
# OAUTH PROVIDERS (configure in Supabase Dashboard)
# =============================================
# Google OAuth: https://console.cloud.google.com/apis/credentials
# GitHub OAuth: https://github.com/settings/developers
